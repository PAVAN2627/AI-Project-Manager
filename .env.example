# Tambo SDK integration will likely require an API key / project identifier.
# Keep this file checked-in and copy it to `.env.local` when wiring real credentials.

# VITE_TAMBO_API_KEY=your_key_here

# Backend (Express)
# Vite dev server proxies `/api/*` to `http://127.0.0.1:5174` by default.

# PORT=5174

# Azure OpenAI (no keys hardcoded; set these locally in `.env.local`)
# AZURE_OPENAI_API_KEY=your_key_here
# AZURE_OPENAI_CHAT_COMPLETIONS_URL=https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT/chat/completions?api-version=<api-version>
